# Copy this file to .env and fill in real values. The .env file is gitignored.
# Used by docker-compose.yml via ${VAR} substitution.
POSTGRES_USER=draftpilot
POSTGRES_PASSWORD=change-me
POSTGRES_DB=draftpilot
